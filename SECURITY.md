# 安全政策

## 支持范围

当前仅维护最新主分支。项目是本地、只读的研究软件，不含交易下单能力。安全问题包括：

- API 密钥、券商凭据、个人持仓或授权数据意外进入日志、报告或版本库；
- MCP 绕过只读边界、返回原始授权数据或监听非本机地址；
- 路径穿越、SQL 注入、任意文件读取或依赖供应链问题；
- 研究档案被未授权修改，导致历史结论失真。

## 私下报告

仓库发布到 GitHub 后，请优先使用 GitHub 的 **Security → Report a vulnerability**
私下提交。公开仓库尚未启用安全公告前，请不要在公开 Issue 中粘贴密钥、账户信息、
真实持仓、授权数据样本或可直接利用的漏洞细节。

报告请包含受影响版本、复现步骤、实际影响和建议修复方式。维护者确认前，不承诺固定
响应时限。

## 凭据处理

- 密钥只放操作系统钥匙串、部署平台的 Secret Store 或本地环境变量；
- 不把真实值写入 `.env.example`、TOML、测试夹具、截图、Issue 或聊天记录；
- 一旦密钥在聊天、日志或提交中出现，立即在提供方控制台吊销并生成新密钥；
- Git 历史中的密钥不能靠“再删一个提交”视为安全，仍必须轮换。

## MCP 边界

MCP 默认只监听 `127.0.0.1`，只提供数据状态、临时组合计算和读取最近档案。它不返回
原始 CSMAR 行、新闻正文、持仓和凭据，也没有下单工具。把 MCP 通过隧道或公网暴露前，
必须配置 HTTPS、访问控制、速率限制和日志脱敏；多人使用时应增加独立身份认证与数据
隔离，不能共享个人账户令牌。

`ASHARE_MCP_ALLOW_LICENSED_DERIVED_RESULTS` 默认关闭。只有在确认数据许可允许外部模型
处理衍生研究结果后，才能在本机私有环境中开启。
