# 参与贡献

感谢改进 A Share Lab。项目优先保证可复现、无前视、只读研究和数据许可边界，不以凑出
股票名单或漂亮收益数字为目标。

## 开发环境

```bash
python3.12 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install -e ".[dev]"
.venv/bin/pytest
.venv/bin/ruff check .
```

需要验证 ChatGPT/Codex 的 MCP 入口时，再安装可选依赖：

```bash
.venv/bin/python -m pip install -e ".[mcp,dev]"
```

## 提交要求

1. 先写清目标、数据时点、失败条件和不会做什么。
2. 修复或新增规则必须有确定性测试；涉及回测时增加防前视测试。
3. 不降低 ST、退市、停牌、不可成交、数据缺失和过热过滤门来凑满组合。
4. 所有收益、概率、Sharpe 和回撤结论必须标注样本、窗口与是否样本外校准。
5. 运行完整测试和 Ruff；网络集成测试应显式标记，不得成为离线测试的隐含依赖。
6. 一个提交只处理一个可审计主题，避免同时混入格式化、数据更新和模型调整。

## 禁止提交的数据

不得提交或附在 Issue/PR 中：

- CSMAR、iFinD、Choice、Infoway、LongBridge 或其他提供方的原始/缓存/导出数据；
- Parquet、DuckDB、SQLite 研究库、Excel/ZIP 原始导出和生成报告；
- API 密钥、OAuth 令牌、券商凭据、真实账户和持仓；
- 未获授权的新闻正文、研报正文或付费数据库内容。

测试使用最小合成夹具。需要复现真实数据问题时，只提供字段结构、脱敏统计和不可逆哈希。

## 许可与来源

贡献默认按 Apache License 2.0 提交。复制或修改第三方代码前，先确认其许可证兼容并保留
版权、归属和修改说明。不要假定“网页公开可见”就代表允许批量抓取、缓存、模型处理或
再发布。

## MCP 贡献

MCP 工具必须保持任务单一、参数有界、返回最小化，并准确标记为只读、非破坏性。
新增写操作、账户数据或公网部署不属于普通 PR：必须先完成身份认证、授权、威胁模型和
隐私审查。
